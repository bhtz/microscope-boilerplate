@using Microscope.Boilerplate.Clients.Web.Blazor.Material.Components.Layout
@using Microscope.Boilerplate.Clients.Web.Blazor.Material.Components.Pages.Common
@using Microscope.Boilerplate.Clients.Web.Shared.Components

<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(Program).Assembly" NotFoundPage="@typeof(NotFoundPage)">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="routeData" DefaultLayout="typeof(MainLayout)">
                <NotAuthorized>
                    @if (context.User.Identity is { IsAuthenticated: false })
                    {
                        <RedirectToLogin />
                    }
                    else
                    {
                        <RedirectTo Path="/access-denied" />
                    }
                </NotAuthorized>
            </AuthorizeRouteView>
            @* <FocusOnNavigate RouteData="routeData" Selector="h1"/> *@
        </Found>
    </Router>
</CascadingAuthenticationState>